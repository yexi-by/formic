# AGENTS.md

本文件只保存 Formic 长期有效的工程规则与代码编写判断方法。当前命令、参数、模块名、
数据字段和具体工作流由代码、测试与现行文档负责，不在此重复。

> 总纲：从真实用户结果反推纵向切片，从当前职责推导直接依赖；每项规则、状态和不变量
> 都有唯一语义所有者，任何调用都必须经过它；只设计当前目标及其直接依赖，按真实的
> 相同语义复用，不为猜测中的未来预留概念。
>
> 可拓展性通过删减获得，不通过预留获得：内核概念最少、职责单一、契约稳定，新能力
> 在边界之外生长。以拓展性为名预留概念，与以未来为名预留概念一样违反此纲。

## 1. 从结果反推目标与契约

开始实现前至少回答：

1. 谁在什么场景下需要这个结果；
2. 成功、失败和必须保持的行为是什么；
3. 哪个模块对结果负最终责任；
4. 当前目标真正需要哪些直接能力；
5. 本次任务在哪里停止，什么证据能够证明完成。

契约从调用方视角说明意图、结果、成功与失败，以及提供方承担的复杂性。函数、领域类型、
模块公开接口或 trait 都可以表达契约，采用满足当前调用关系的最简单形式；只有存在真实
的替换、隔离、独立测试或多实现需要时才引入抽象。

接口用职责语言表达高级意图；算法、存储、第三方类型、资源限制和调度细节留在实现内部。

项目进度以用户可观察的真实结果衡量；目录、模块、crate 或组件的数量不代表进度。

## 2. 依赖与模块边界

项目依赖是能力网络，不是必须逐层经过的阶梯。判断直接依赖是否成立时确认：

1. 当前模块对该能力是否存在独立需要；
2. 调用是否只依赖对方职责级的稳定契约；
3. 是否绕过其他模块拥有的规则、状态或不变量；
4. 候选中间层是否增加语义转换、策略、保护或生命周期责任；
5. 依赖是否保持单向且不形成循环。

中间层只改名或转发调用时，直接依赖更准确；中间层拥有不可替代的业务含义时，通过它
协作。Composition Root 可以直接使用具体实现完成构造、配置和生命周期管理，业务执行
仍通过相应契约完成。

## 3. 复用与拆分

编写具体实现前检查现有能力，并按语义作出唯一判断：

- 语义、规则、不变量、错误语义和生命周期一致时，直接复用同一实现；
- 只有部分职责一致时，提炼最小、可命名且能独立验证的公共能力，并在同一次改动中
  迁移全部现有调用方；
- 只有代码形状或步骤相似时，保持独立并明确真实语义差异。

拆分只有在以下情况成立：单元具有独立职责、不变量或变化原因，当前确有调用方需要；
或形成必要的依赖、编译、发布或所有权隔离。文件较长、目录整齐、层级对称、形式上能
拆开、猜测将来复用，都不是拆分理由。共同维护同一不变量、总是一起变化的代码保留在
同一模块。

共享接口只随当前调用方或已确认后续调用方的明确需要扩大，不根据猜测的未来场景增加
字段、状态或抽象。

## 4. 数据建模与类型

代码和数据用尽可能少的概念完整表达当前真实语义。每个类型、字段、状态和接口都必须
能够说明由谁负责、哪些当前调用方使用、消除了什么复杂性；不为未来需求预留概念。

进程内部优先使用所有权明确、能保证不变量的领域类型：互斥模式用枚举表达，组合事实
用字段私有的值类型和构造函数建立不变量。文本树、字符串标签和松散键值只停留在解析、
序列化或持久化边界，进入内部前转换成已校验的类型。

面向人类或模型的外部格式也是产品接口：只保留当前意图需要的字段，优先使用成熟、易读、
适合批量生成和审核的标准。外部不透明字段只有在当前流程确实读取、写回或原样转发时
才保留。

## 5. 单一当前版本

业务模型、输入输出协议、持久化格式和配置不建立版本概念。Formic 只实现当前契约；
确认变更后，在同一次改动中替换生产代码、测试、夹具、配置、文档、错误和诊断，并删除
兼容解析、旧字段别名、格式探测、迁移、双读双写、回退和旧版本专用提示。无效输入只按
现行契约处理，不识别或提示其历史身份。

## 6. 面向人的标识与输出

CLI 参数与输出、配置、导出文件、文件名、日志、错误信息和文档示例，不得要求或展示
hash、UUID、数据库随机主键或其他无法直接理解的标识。优先使用源文件名、自然编号、
字段名和简短业务名称描述对象。内部实现可以使用 hash 或数据库 ID，但不得暴露到用户
需要阅读、编辑或传回程序的输入输出中。

错误输出只说明对象、原因和修改方法，不倾倒内部状态、实现阶段、错误码、数据库行或
供应商请求标识。不得为了看似严谨而增加没有实际作用的字段、提示、确认或诊断消息。

临时文件、日志、任务记录使用目标文件名、命令名或自然编号命名，不使用随机字符串；
并发安全由原子创建或锁保证，不把随机名称当作并发机制。

## 7. 配置与资源参数纪律

配置只收纳部署环境、外部服务或操作者能够长期选择的策略；固定产品语义由代码契约规定；
并发度、窗口、批次、连接复用和缓存等内部参数由执行代码根据基准和运行数据决定。没有
实际选项的模块不建立配置对象；缺少必填外部选择时明确失败。

只有外部硬性限制、可复现故障或测量证据，才能支持并发上限、队列、超时、批量、重试、
等待窗口、容量拒绝或磁盘暂存。

不自行限制任务总量。文件、任务、结果等业务对象不设置人为上限；活动窗口满时让后续
工作等待。真实的文件系统、操作系统、内存、外部协议错误必须完整报告，不能用一个很大
的常量伪装成「无上限」。

## 8. 错误语义

每项业务事实、规则和不变量都有唯一责任方。最了解外部输入的边界负责解析、校验并转换
为能保证不变量的内部类型；转换成功后，内部模块直接信任该契约，同一事实不重复校验。
会随时间变化的外部状态，由真正执行副作用的模块在操作前重新观测。

输入错误、环境失败和内部不变量破坏保持不同含义。产生错误的责任边界使用类型化内部
错误保存控制流所需事实；进入 CLI、日志等面向人的呈现前，转换成对象、直接原因和修改
方法，不解析 Display 文本猜测事实。

业务允许的未产出、Partial 和 Unavailable 是正常结果，仍带有足够的结构化诊断；只有
模块无法继续履行契约或保持状态一致时才返回错误。

## 9. 日志与可观测性

日志、指标、追踪和任务记录默认只是业务行为的附属证据，不参与提交、恢复、重放或副作用
准入。只有契约明确要求「证据完整才允许操作或成功」时，记录才成为业务状态。

事件名称、正文和字段只描述已经确认的事实：阶段完成后才能记录完成事件，统计事件使用
不表示提交或成功的中性含义，普通进度不得提前宣称成功。任何用户可见诊断都不能被空
错误分支或无诊断的退化吞掉；记录失败必须及时产生用户可见诊断，但不改写已经确定的
业务结果和退出语义。

## 10. 状态、持久化与并发

只有后续行为确实需要跨时间、进程或阶段继续使用的事实才进入持久化；能够从权威事实
稳定推导的数据在使用时计算。每项持久化事实必须明确写入方、读取方、权威位置，以及
创建、更新和失效时机。

批量工作中每项有自己的成败：已确认且一致的进展及时保存，未完成部分由后续运行根据
权威状态重新推导。项目总工作量与同时处理的工作量严格区分：窗口只限制正在处理的工作，
窗口满时排队等待，不把合法总量变成容量错误。

内存优先用于索引、缓存和保存已完成结果；磁盘暂存或逐条往返只有在内存问题得到证实
时才引入。并发可以改变完成时间，不能改变自然顺序、分组结果、提交顺序、错误语义或
持久化一致性。

## 11. 实现与验证顺序

实现顺序与设计顺序一致：

1. 用高层行为固定当前目标的可观察结果和失败语义；
2. 按调用方需要定义直接依赖的最小契约；
3. 实现并验证当前目标，范围外依赖使用测试替身；
4. 依赖进入实现范围后，再以其上游契约为起点实现和验证；
5. 任务要求从入口到结果时，用最小真实样本通过生产入口执行完整路径；
6. 共享能力存在多个调用方时，验证各方依赖同一语义和同一实现。

契约完成、局部测试通过或底层组件就绪，都不能单独证明纵向切片完成。交付时分别说明
各层的完成程度；生产入口只连接已经真实实现并完成相应验证的能力。尚未实现、连接或
验证的范围必须明确说明。

## 12. 测试纪律

进程级主成功测试和端到端测试使用正式默认行为；可选行为另设选择测试，不能为了简化
公共夹具而改变主路径的正式默认。

测试断言具体诊断、持久记录、状态影响和退出码，不只断言没有 panic；必要事件恰好出现
一次，失败路径不能出现完成、提交或成功文案。

生产代码和测试套件不得读取、解析或断言 Markdown 文档、文档示例、固定文字或发行副本
内容；发现此类测试时直接删除，不用快照或字符串断言替代。自动化测试只验证产品可观察
行为与数据契约。

## 13. 重构与同改原则

既有结构无法表达当前语义、产生重复知识或持续阻碍实现时，进行范围明确的重构：在同一
次改动中更新全部当前调用方、测试、配置和文档，并删除失效实现；不保留转接层、新旧双
模型或历史版本专用错误。

源码、测试与规格冲突时，先确认当前有效契约，再修正不符合契约的一方；规格不为迁就
现有代码而改写。候选设计必须明确标注为候选，文档只描述已经实现并验证的事实。

## 14. LLM 调用边界

Formic 的核心循环是「LLM ↔ 工具调用」，LLM 本身是黑箱，但进出黑箱的内容必须完全
可控、可查、可复现：

- 每次调用的输入构造（数据分片、上下文、任务说明、格式要求）和原始输出都必须完整
  记录、可检查、可追踪；
- 发给模型的内容只携带当前输出不可缺少、且尚未由系统上下文确定的事实；能按顺序、
  分组或已校验状态稳定推导的内容不重复编码；
- 任何新增模型输入内容都必须说明模型如何使用它，以及缺少它为什么会使任务无法正确
  完成；
- 问题定位必须能够精确到「输入如何构造、输出在哪一步不符合约束」，同类问题能够稳定
  复现。

## 15. Review 与质量防线

个人借助 Agent 推进项目时，Review 的重心是结果而非逐行代码：

- 以真实大规模负载做压力测试，主动寻找系统失效边界，不用小 Demo 证明系统可用；
- 暴露出的问题转化为测试用例、校验规则和自动化防线，防止同类错误再次发生；
- 验证标准是最终结果是否可用、关键输入输出是否可控、问题是否能够快速定位与回归。

## 16. 每次任务的方向检查

设计、实现和评审至少检查：

1. 当前工作服务什么用户结果或上游承诺，谁拥有其中的规则和不变量；
2. 直接依赖为什么必要，是否绕过语义所有者或保留了纯转发层；
3. 复用和拆分是否依据真实相同语义以及当前或已确认的调用方，而不是代码形状或未经
   确认的未来设想；
4. 新增类型、字段、状态、配置、批处理或资源机制分别由什么真实读写关系、外部限制或
   测量证据证明，是否误设任务总量上限；
5. 外部事实是否只在责任边界校验一次，错误是否保留具体原因和最终状态，可观测性是否
   仍未参与未经契约授权的业务判断；
6. 真实生产入口、正式默认、失败、取消、部分结果、生命周期和副作用是否得到相称验证；
7. 受影响范围内的源码、测试和文档是否描述同一个当前结果，是否存在旧实现残留；
8. 现有证据是否足以证明纵向结果，而不只是局部代码能够运行；
9. LLM 调用的输入输出是否完整可控、可追踪、可复现。

这些问题无法回答时，先缩小目标和边界。任何技术选择都必须能够沿依赖关系追溯到真实
需求。
