# AGENTS.md

本文件只保存 Formic 长期有效的宏观维护指南。模块职责、配置字段、命令、测试矩阵和发布步骤属于 `docs/`，不要把这些易变化细节重新堆回本文件。

## 项目目标

Formic 面向大量能够独立完成、独立重试和独立发布的语义任务。调用方负责分片规划与跨单元归并；Formic 负责单元级多轮 `LLM ↔ 工具` 执行、资源调度、取消、恢复、原子发布和可审计证据。

项目进度以真实用户结果衡量。目录、模块、抽象或测试数量本身不代表能力完成。

## 长期边界

- 单元正确性不依赖其他 worker 的完成顺序或共享可变状态；并发只改变耗时，不改变结果含义。
- worker 是受能力契约约束的微型 agent。模型、内置工具、MCP、输入和已发布结果的可见性必须显式、可检查，普通工具统一经过唯一调度入口。
- LLM 是黑箱，但进入模型的语义、通过验收的输出、工具往返和完成事实必须可追踪；秘密、传输 envelope、无效 payload 和图片编码不得进入公开档案。
- 每项规则、状态和不变量只有一个语义所有者。调用方负责组合，不复制提供方规则，也不绕过其边界。
- 可扩展性来自少而稳定的内核概念和清晰边界。只实现当前目标及直接依赖，不为猜测中的未来预留字段、模式、抽象或兼容层。
- Formic 只实现一个当前契约。确认外部格式或行为变化后，同步替换实现、测试、配置和文档，不长期维护新旧双版本。
- 结果、作业身份和恢复状态必须保持一致且可核对。已发布结果不可覆盖；单元失败不回滚其他已确认进展。
- 资源窗口限制活动工作，不限制合法任务总量。任何固定容量、超时、重试或并发选择都需要外部限制、可复现故障或测量证据。

## 维护方向

- 从用户结果反推完整纵向切片，先定位规则所有者，再确定最小完整改动和全部受影响调用方。
- 复用只依据相同语义、错误、不变量和生命周期；只有代码形状相似时保持独立。
- 外部输入在最了解它的边界解析并转为可保证不变量的内部类型，内部不重复猜测或解析显示文字。
- 输入错误、环境失败、远端失败和内部不变量破坏保持不同含义。面向人的诊断只说明对象、直接原因和修改方法。
- 配置只承载部署环境、外部服务和操作者可长期选择的策略；固定产品语义由代码拥有。
- 可观测性默认是行为证据，不参与准入、恢复或发布判断；只有当前契约明确要求证据完整时才成为业务条件。
- 测试验证生产可观察行为、失败语义、状态和副作用，不读取 Markdown 文档或实现细节作为业务权威。
- 共享契约变化时，同一次改动覆盖真实入口、全部当前调用方、配置、文档和相称的端到端验证。

## 文档路由

开始维护前先阅读：

- `docs/project-map.md`：找到生产入口、模块职责、规则所有者和数据流；
- `docs/maintenance.md`：按改动类型执行具体维护、验证和发布流程。

按任务补充阅读：

- `docs/usage.md`：CLI、配置、计划、工具、输出和续跑的当前用户契约；
- `docs/task-design.md`：独立单元边界与提示词设计；
- `docs/observability.md`：运行档案、统计、状态和排错；
- `docs/README.md`：完整文档分工。

README 面向首次使用者，docs 面向详细使用和维护，AGENTS 只保留宏观项目规则。重复内容应归还其权威文档，其他位置只保留概括和链接。

## 交付原则

实施前核实目标、事实前提、因果关系和停止边界；实施后核对真实差异、受影响入口和验证证据。高并发、恢复、协议或持久状态变化必须使用与风险相称的真实入口和规模证据。

保留用户已有改动，不覆盖、回滚或夹带无关差异。文档只描述已经实现并验证的当前行为；未实现、未连接或未验证的范围必须明确说明。
